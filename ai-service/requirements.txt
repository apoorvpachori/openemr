# Web framework + ASGI server
fastapi==0.115.12
uvicorn[standard]==0.34.2

# HTTP client — used in Step 3 to call OpenEMR endpoints from Python
httpx==0.28.1

# AI layer — wired in Steps 5-7
anthropic==0.97.0
langgraph==1.1.10
langchain-core==1.3.2
langchain-anthropic==1.4.2
langchain-openai==1.2.1
langsmith==0.7.38

# Data validation (FastAPI uses this internally; also used for agent state models)
pydantic==2.11.4
