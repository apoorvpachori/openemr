# Copy this file to .env and fill in values before deploying.
# Never commit .env to version control.

# MariaDB
MYSQL_ROOT_PASS=change_me_root
MYSQL_PASS=change_me_openemr

# OpenEMR admin UI password
OE_PASS=change_me_admin

# AI service — add when Python service is deployed
ANTHROPIC_API_KEY=sk-ant-...
LANGSMITH_API_KEY=lsv2_...
